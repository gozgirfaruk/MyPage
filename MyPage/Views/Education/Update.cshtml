
@model MyPage.Models.Entity.Education

@{
    ViewBag.Title = "Update";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<body class="bg-gradient-primary">

    <div class="container">

        <!-- Outer Row -->
        <div class="row justify-content-center">

            <div class="col-xl-12 col-lg-12 col-md-12">

                <div class="card o-hidden border-0 shadow-lg my-5">
                    <div class="card-body p-0">
                        <!-- Nested Row within Card Body -->
                        <div class="row">
                            <div class="col-lg-12">
                                <div class="p-5">
                                    <div class="text-center">
                                        <h1 class="h4 text-gray-900 mb-4"> Eğitim Güncelleme Sayfası</h1>
                                    </div>

                                    <form class="user" method="post">
                                        <div class="form-group">
                                            @Html.TextBoxFor(x => x.EducationID, new { @type="hidden"})
                                        </div>
                                        <div class="form-group">
                                            @Html.Label("Tarih")
                                            @Html.TextBoxFor(x => x.Header, new { @class = "form-control form-control-user" })
                                        </div>
                                        <div class="form-group">
                                            @Html.Label("Okul Adı")
                                            @Html.TextBoxFor(x => x.SchoolName, new { @class = "form-control form-control-user" })
                                        </div>
                                        <div class="form-group">
                                            @Html.Label("Okul Ünvanı")
                                            @Html.TextBoxFor(x => x.Degree, new { @class = "form-control form-control-user" })
                                        </div>
                                        <div class="form-group">
                                            @Html.Label("Açıklama")
                                            @Html.TextAreaFor(x => x.Description, new { @rows = 5  , @class= "form-control form-control-user" })
                                        </div>
                                        <br />

                                        <button class="btn btn-primary btn-user btn-block" type="submit">Güncelle</button>
                                    </form>

                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

